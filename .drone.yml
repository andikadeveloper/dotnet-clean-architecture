kind: pipeline
type: exec
name: default

platform:
  os: linux
  arch: amd64
  
steps:
- name: integration-test
  commands:
    - docker build -t dotnet-clean-architecture .
    - docker run dotnet-clean-architecture
trigger:
  branch:
    - main
  event:
    - push
